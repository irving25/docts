// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyASbVGiQoWO7amVqVh_PEHikZEQQZ75AII',
    authDomain: 'peoplerecruitment-e89fd.firebaseapp.com',
    databaseURL: 'https://peoplerecruitment-e89fd.firebaseio.com',
    projectId: 'peoplerecruitment-e89fd',
    storageBucket: 'peoplerecruitment-e89fd.appspot.com',
    messagingSenderId: '729426763619',
    appId: '1:729426763619:web:03c4a22286dc2e37b73d56'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
import 'zone.js/dist/zone-error';  // Included with Angular CLI.

